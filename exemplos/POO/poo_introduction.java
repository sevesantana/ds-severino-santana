
public class poo_introduction {
    /* A Programação Orientada a Objetos (POO) é um paradigma de programação que se baseia na
      ideia de que os programas podem ser modelados como um conjunto de objetos que
      interagem entre si. Java, uma linguagem de programação amplamente utilizada,
      é conhecida por seu forte cumprimento aos princípios do POO. */

    /* O que é uma classe? - Em Java, uma classe é um modelo para criar objetos.
     Ela define atributos (variáveis de instância)
     e métodos (comportamentos) que os objetos da classe possuirão. */
    // Exemplo: public class Carro {}

    /*Agora adicionamos objetos a esta classe como:
      public class Carro {
        int ano;
        String modelo;
        int velocidade;
        String cor;
        }               */

    /* Para cada objeto haverá um atributo(característica):

        Carro meuCarro = new Carro();
        meuCarro.modelo = "Civic";
        meuCarro.ano = 2022;
        meuCarro.cor = "Preto";
        meuCarro.velocidade = 0;
     */

    // E por último os métodos(ações) que vão ser executadas, como frear, acelerar e buzinar.
}


